{% extends "profiles/base.jinja2" %}

{% block title %}
    Сброс пароля
{% endblock %}

{% block content %}
    <h1>Укажите новый пароль</h1>
    {% if validlink %}
        <div class="wrapper">
            <div class="header">
                <h3 class="sign-in">Восстановление пароля (2/2)</h3>
                <h5>Придумайте новый пароль для учетной записи</h5>
            </div>

            <form action="." method="post">
                {% csrf_token %}
                <div>
                    {{ form.new_password1.errors }}
                    <input type="password" name="new_password1" id="name" placeholder="Новый пароль" />
                </div>
                <div>
                    {{ form.new_password2.errors }}
                    <input type="password" name="new_password2" id="name" placeholder="Повторите пароль" />
                </div>
                <div>
                    <input type="submit" value="Сохранить пароль" />
                </div>
            </form>

        </div>
    {% else %}
        <p>Ссылка для сброса пароля недействительна, возможно, потому, что она уже использовалась.
            Пожалуйста, запросите новый сброс пароля.</p>
    {% endif %}
{% endblock %}
